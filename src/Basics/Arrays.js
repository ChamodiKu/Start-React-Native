//declare new array called colours
const colors = ['red', 'green'];
//access different elements inside that array
colors[0];
colors[1];
//access more elements inside the array 
const [colorOne, colorTwo] = colors; //from color array take the first element and assign it to the variable colorOne and second to colorTwo
//access elements
colorOne;
colorTwo;
